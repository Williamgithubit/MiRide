// This file ensures React is properly imported and exported
import * as React from 'react';
import * as ReactDOM from 'react-dom';

// Re-export React and ReactDOM to ensure they're properly bundled
export { React, ReactDOM };
